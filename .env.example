DB_HOST=localhost
DB_PORT=3306
DB_NAME=control_nfc
DB_USER=app
DB_PASSWORD=app_password
DB_ROOT_PASSWORD=root_password

JWT_SECRET_CURRENT=replace_with_secure_secret
JWT_SECRET_PREVIOUS=

# HTTPS / nginx
DOMAIN=api.tudominio.com
CERTBOT_EMAIL=admin@tudominio.com

# Zona horaria del backend (default: America/Argentina/Buenos_Aires)
TZ=America/Argentina/Buenos_Aires
# Puerto en que escucha el backend (default: 3000)
PORT=3000

# CORS (producción): orígenes permitidos separados por coma
CORS_ORIGINS=https://panel.tudominio.com
